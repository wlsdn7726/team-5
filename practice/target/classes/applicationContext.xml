<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd">

<context:component-scan base-package="register.impl"/>

<!-- 변수 설정할 수 있게 홀더 설정 -->
<context:property-placeholder location="classpath:config/db.properties"/>

<!-- 설정된 변수를 이용하여 빈 생성 -->
<bean id="dataSource" 
	class="org.apache.commons.dbcp.BasicDataSource" destroy-method="close">
	<property name="driverClassName" value="${jdbc.driver}"/>
	<property name="url" value="${jdbc.url}"/>
	<property name="username" value="${jdbc.username}"/>
	<property name="password" value="${jdbc.password}"/>
</bean>

	<!-- JDBC 대신 MyBatis 연동 설정 -->
	<!--  
		  1. pom.xml로 mybatis 설치 
		  2. 사용할 쿼리를 모아놓은 board-mapping.xml을 생성
		  3. mybatis-config.xml로 mybatis 초기 설정
	-->
	<bean id="sqlSession" class="org.mybaties.spring.SqlSessionFactoryBean">
	<!-- DB 연결하는 설정을 참조 -->
	<property name="dataSource" ref="dataSource"/>
	<!-- mybatis의 성정 파일을 연결  -->
	<property name="configLocation" 
		value="classpath:mybatis-config.xml"/>
	</bean>
	
	<!--  DAO에 sqlsession을 호출할 때 연결객체를 주입 -->
	<bean class="org.mybaties.spring.SqlSessionTemplate">
		<constructor-arg ref="sqlSession"/>
	</bean>
	


</beans>
